  services:
  - type: web
    name: hexfront-server
    runtime: node
    dir: server
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
